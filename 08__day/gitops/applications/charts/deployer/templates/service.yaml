apiVersion: v1
kind: Service
metadata:
  name: {{ include "deployer.fullname" . }}
  labels:
    app: {{ include "deployer.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: health
      protocol: TCP
      name: health
  selector:
    app: {{ include "deployer.name" . }}
