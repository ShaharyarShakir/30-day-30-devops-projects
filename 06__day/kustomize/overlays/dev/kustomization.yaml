apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: url-shortener-dev

resources:
  - ../../base

helmGlobals:
  chartHome: ../../../helm

helmCharts:
  - name: url-shortener
    releaseName: url-shortener
    valuesFile: values.yaml
