# Env / secrets
.env
*.env.local

# Python
__pycache__/
*.pyc
.venv/
venv/

# Go
/backend/**/bin/
/backend/**/*.exe

# Node (frontend)
node_modules/
dist/
build/

# DVC
/ml/datasets/raw/*
/ml/datasets/processed/*
!/ml/datasets/raw/.gitkeep
!/ml/datasets/processed/.gitkeep

# MLflow local artifacts (we use MinIO instead)
mlruns/
mlflow.db

# OS / editor
.DS_Store
.vscode/
.idea/

# Docker volumes are managed by Docker, not git
